# fast-food-management-database-project
The Fast Food Database Project uses relational database concepts such as tables with primary and foreign keys. The database is normalized to reduce redundancy. SQL commands are used for data creation and manipulation, and constraints and joins ensure data integrity and meaningful data retrieval.
